#include "3-calc.h"
/**
 * op_add - add to numbers
 * @a:the first integer
 * @b:the second integer
 * Return:return the add of the numbers
*/
int op_add(int a, int b)
{
return (a + b);
}

/**
 * op_sub - sub of two numbers
 * @a:the first integer
 * @b:the seconf integer
 * Return:return the sub
*/
int op_sub(int a, int b)
{
return (a - b);
}
/**
 * op_mul - mulltipliplicate two numbers
 * @a:the first integer
 * @b:the seconf integer
 * Return:return the mulltiplacation of the two num
*/
int op_mul(int a, int b)
{
return (a * b);
}
/**
 * op_div - division of two numbers
 * @a:the first integer
 * @b:the second integer
 * Return:return the division of the two num
*/
int op_div(int a, int b)
{
return (a / b);

}
/**
 * op_mod - modulo division of two numbers
 * @a:the first integer
 * @b:the second integer
 * Return:return the mod div of the two num
*/
int op_mod(int a, int b)
{
return (a % b);
}