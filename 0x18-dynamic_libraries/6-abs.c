#include "main.h"

/**
 * _abs - Prints an integer to stdout
 *
 * @k: The integer to be printed
 *
 * Return: void
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	else
	{
		return (k * -1);
	}
}

